<div class="body">
  <div class="container">
    <div class="row">
      <%= render'books/list' %>
      <p id="notice"><%= notice %></p>
      <div class="col-xs-9">
        <h2>Books</h2>
          <table class="table  table-hover">
            <thead>
              <tr>
                <th></th>
                <th>Title</th>
                <th>Opinion</th>
              </tr>
            </thead>
                  </th>
            <% @books.each do |book| %>
                <tr>
                  <th><%= attachment_image_tag @user, :profile_image,size: "40x40",format: 'jpeg', fallback:"no_image.jpg" %>
                  </th>
                <th> <%=link_to book.title, "/books/#{book.id}" %>

                </th>
                <th><%= book.body %></th>
              </tr>
            <% end %>
          </table>
      </div>
    </div>
  </div>
</div>