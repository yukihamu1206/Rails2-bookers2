<div class="body">
    <div class="container">
	<div class="row">
		<%= render'books/list' %>
		<% if @post_book.errors.any? %>
  <div id="error_explanation">
    <h2><%= @post_book.errors.count %>errors prohibited this obj from being saved:</h2>
 
    <ul>
    <% @post_book.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
			<div class="col-xs-9">
				<h2>Books</h2>
					<table class="table  table-hover">
						<thead>
							<tr>
								<th></th>
								<th>Title</th>
								<th>Opinion</th>
							</tr>
						</thead>
					    		</th>
						<% @books.each do |book| %>
					    	<tr>
					    		<th><%= attachment_image_tag book.user, :profile_image,size: "40x40",format: 'jpeg', fallback:"no_image.jpg" %>
					    		</th>
								<th> <%=link_to book.title, "/books/#{book.id}" %>

								</th>
								<th><%= book.body %></th>
							</tr>
						<% end %>
					</table>
			</div>
		</div>
	</div>
</div>